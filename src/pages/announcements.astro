---
import BaseLayout from '../layouts/BaseLayout.astro';
import AnnouncementsList from '../components/react/AnnouncementsList.tsx';
import { NEIGHBORHOOD } from '../lib/site';
---

<BaseLayout
  title="Announcements"
  description={`Community announcements and news for ${NEIGHBORHOOD}.`}
>
  <div class="page-section">
    <p class="eyebrow">Community</p>
    <h1 class="page-title">Announcements</h1>
    <p class="page-lead">
      News and reminders from the Ashebrook board, newest first.
    </p>

    <AnnouncementsList client:only="react" variant="page" />
  </div>
</BaseLayout>
