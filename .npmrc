# npm 12 refuses dependencies fetched from a URL by default. `xlsx` is pinned in
# package.json to the SheetJS CDN tarball (the registry copy is abandoned), with
# its sha512 in package-lock.json. `root` admits URL specs declared in this
# package.json only, never one pulled in transitively. Older npm ignores it.
allow-remote=root
